import { Card, CardFooter } from '@/ui/card.ripple';

import { Button } from '@/ui/button.ripple';

export component ButtonDemo() {
	<div $class='w-full max-w-sm'>
        <Button $type='submit' $variant="default" $class='w-full'>{'Submit'}</Button>
    </div>
}